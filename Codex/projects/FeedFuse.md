# FeedFuse

## 2026-05-27

- 项目位于 `E:\learn\FeedFuse`，Next.js 16 + pnpm。
- 本次检查发现完整测试失败集中在 `readerSnapshotService` 的 cursor/previewImage 单测：测试未 mock `articleTagsRepo`，新增标签统计查询消耗了 `pool.query` mock，导致 total count 查询返回 `undefined`。
- 已在相关单测中隔离 `listTagsForArticles` 和 `listTagsWithVisibleArticleCounts`，业务代码未改。
- 验证通过：`pnpm test`、`pnpm lint`、`pnpm type-check`、`pnpm build`。
- 继续完善 SOCKS 外联代理配置：`FEEDFUSE_OUTBOUND_PROXY` 已补到本地/发布 env 示例、发布 compose、开发/部署文档；`externalHttpClient` 对空值忽略，对非法 URL 和非 SOCKS 协议给出稳定错误；新增配置闭环测试。
- Reading Efficiency Phase 2（选择模式 + 批量操作）当前已落地并完成验证；聚焦测试 `pnpm test -- src/test/server/repositories/articlesRepo.bulkPatch.test.ts src/test/app/api/articles/routes.test.ts src/test/lib/apiClient.test.ts src/test/lib/userOperationCatalog.test.ts src/test/store/appStore.test.ts src/test/features/articles/ArticleList.test.tsx src/test/features/reader/ReaderLayout.test.tsx` 通过 7 个文件 / 262 个用例，`pnpm type-check`、`pnpm lint`、`pnpm build` 通过。
- Google News RSS 代理收尾验证：worker 容器内 `FEEDFUSE_OUTBOUND_PROXY=socks5://host.docker.internal:7898` 已注入，`host.docker.internal:7898 -> 127.0.0.1:7897` 端口桥可 TCP 连通；用应用真实 `fetchRssXml` 路径抓 `news.google.com` 仍失败，错误为 TLS 建立前 socket 断开。宿主和容器用同一 SOCKS 代理访问 `example.com` 成功，但访问 `www.google.com` / `news.google.com` 均失败；结论是当前本机代理出口/规则没有提供可用 Google 通道，FeedFuse 代码链路不是当前阻塞点。下一步：在 Clash Verge/Mihomo 中切换可访问 Google 的节点或规则后，复跑 worker 内 `fetchRssXml` 验证。
- 2026-05-28 Google News RSS 恢复闭环：代理出口切换/恢复后，宿主 SOCKS 可访问 `www.google.com` 和 `news.google.com/rss`；worker 容器内应用真实 `fetchRssXml` 路径返回 200，真实 `fetchFeedXml + parseFeed` 对 feed `3` 解析出 102 条。随后触发 feed `3` / `7` 真实刷新任务，run `18` / `19` 均 succeeded；`feeds.last_fetch_status=200` 且 `last_fetch_error` 为空，`articles` 中 feed `3` / `7` 各保留 500 条。
- 2026-05-28 收尾验证：当前未提交改动（SOCKS 外联代理配置、`externalHttpClient` 测试、readerSnapshotService cursor/previewImage 测试隔离）已通过聚焦测试 `pnpm test -- src/test/server/http/externalHttpClient.test.ts src/test/config/outboundProxyConfig.test.ts src/test/server/services/readerSnapshotService.cursor.test.ts src/test/server/services/readerSnapshotService.previewImage.test.ts`（4 文件 / 20 用例）、`pnpm type-check`、`pnpm lint`、`pnpm build`。仓库仍在 `main`，本地 ahead 2，相关改动尚未提交。
- 2026-05-29 提交：已将 SOCKS 外联代理支持、相关文档/配置、readerSnapshotService 测试隔离提交为 `7c68db6 fix: support outbound proxy for external fetches`；提交前验证通过：聚焦测试 4 文件 / 20 用例、`pnpm type-check`、`pnpm lint`、`pnpm build`。本地 `main` ahead 3。
- 2026-05-29 修复文章详情图片破图：媒体代理 SSRF 安全检查现在允许“公有域名解析到 198.18/198.19 代理占位地址”的场景，同时仍拒绝直接访问 198.18/198.19 IP 和私网地址；IBM Research 图片 `/api/media/image` 已验证返回 `200 image/jpeg`，相关媒体/API 测试与 `pnpm type-check` 通过，web 容器已重建。
- 2026-05-29 标签管理功能分支 `codex/tag-management` 已复用并合入当前 `main` 的代理修复提交，分支头为 `8b63f51`。标签管理实现包括颜色预设、标签重命名/改色/删除 API、store 乐观更新、侧边栏右键菜单与文章列表/详情颜色渲染；验证通过：聚焦标签管理测试 9 文件 / 273 用例、完整 `pnpm test` 214 文件通过 / 1086 用例通过（1 文件/4 用例 skipped）、`pnpm type-check`、`pnpm lint`、`pnpm build`。当前主工作区 `main` 仍有文章图片破图修复的未提交改动，因此标签管理尚未合回 `main`。
- 2026-05-29 标签管理功能已按用户选择本地合并回 `main`，当前 `main` 指向 `8b63f51`，本地较 `origin/main` ahead 11；临时 worktree `.worktrees/tag-management` 和本地分支 `codex/tag-management` 已清理。合并后验证通过：完整 `pnpm test` 214 文件 / 1088 用例通过（1 文件/4 用例 skipped）、`pnpm type-check`、`pnpm lint`、`pnpm build`。主工作区仍保留文章图片破图修复的未提交改动：`mediaProxyGuard.ts` 与对应测试。
- 2026-05-29 提交文章图片破图修复：`6221679 fix(media): allow proxy placeholder DNS for images`。提交内容限定为 `mediaProxyGuard.ts` 和对应测试，允许公有域名解析到 198.18/198.19 代理占位地址但仍拒绝直接占位 IP；提交前验证通过：媒体/API 聚焦测试 4 文件 / 79 用例、`pnpm type-check`。提交前曾因清理 worktree 后 `node_modules` 链接断裂导致验证失败，已用 `pnpm install --force` 重建依赖后复跑通过。当前 `main` 较 `origin/main` ahead 12，工作区干净。
- 2026-05-29 已按用户确认推送 `main` 到远端：`git push origin main` 成功，远端 `origin/main` 从 `ec9a4fa` 更新到 `6221679`；本地 `main...origin/main` 已同步，工作区干净。
- 2026-05-29 批量标签操作已按 spec 实现：新增 `POST /api/articles/bulk-tags`，支持对当前选择的文章批量添加已有/新标签、批量移除标签；前端选择工具栏新增“添加标签/移除标签”弹窗，store 会同步可见文章、详情缓存、标签计数和 tag 视图移除行为。2026-05-30 收尾补齐：remove 请求校验失败会记录 `articleTag.bulkRemove`，批量移除弹窗打开时不再默认选中第一个标签，需用户点选后“确认移除”才启用。验证通过：聚焦测试 `pnpm test -- src/test/server/repositories/articleTagsRepo.test.ts src/test/app/api/articles/routes.test.ts src/test/lib/apiClient.test.ts src/test/store/appStore.test.ts src/test/features/articles/ArticleList.test.tsx`（5 文件 / 278 用例）、`pnpm type-check`、`pnpm lint`、`pnpm build`。
- 2026-05-29 批量标签操作补充浏览器验证：重建并重启 `feedfuse-web` 后，Playwright 通过本地测试会话访问 `http://localhost:9559/?article=535805`，确认已登录、文章列表加载 17 行、选择模式打开、选择当前列表后选中 17 篇、批量添加标签按钮可见且可打开弹窗，输入标签名后“确认添加”启用；当前数据没有可移除标签，因此批量移除按钮按设计禁用。另补 `.dockerignore` 排除 `.pnpm-store`、`.playwright-cli`、`output`，避免 Docker 构建上下文继续携带约 1.9GB 的本地 pnpm store；复跑 `docker compose build web` 后上下文从约 1.52GB 降到 29.07KB，2026-05-30 再次 `docker compose up --build -d web` 后上下文约 421.82KB，`feedfuse-web` healthy，`/api/health` 200。
