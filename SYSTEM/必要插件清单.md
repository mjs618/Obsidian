---
type: 配置
domain: 知识管理
status: 进行中
tags:
  - 领域/知识
  - 类型/配置
---

# 必要插件清单

本页记录当前 Dusk 风格工作台实际依赖的插件。迁移、重装或排查首页异常时，优先检查这里。

## 首页必需

| 插件                         | 用途                 | 当前状态   |
| -------------------------- | ------------------ | ------ |
| Dataview                   | 首页统计、任务摘要、最近更新     | 已安装并启用 |
| Meta Bind                  | 首页按钮 `BUTTON[...]` | 已安装并启用 |
| Tabs                       | 首页底部分类视图           | 已安装并启用 |
| Omnisearch                 | 全库搜索入口             | 已安装并启用 |
| Recent Files               | 最近文件入口             | 已安装并启用 |
| Hotkeys for specific files | 首页按钮直达固定页面         | 已安装并启用 |
| Homepage                   | 启动后进入 `HUB/Home`   | 已安装并启用 |
| View Mode by Frontmatter   | HUB 页面默认阅读视图       | 已安装并启用 |

## 工作流增强

| 插件 | 用途 | 当前状态 |
| --- | --- | --- |
| Tasks | 全库任务识别和查询 | 已安装并启用 |
| QuickAdd | 快速捕获和自动化入口 | 已安装并启用 |
| Templater | 模板自动化 | 已安装并启用 |
| Calendar | 日记导航 | 已安装并启用 |
| Obsidian Git | 版本备份 | 已安装并启用 |
| Excalidraw | 图形化笔记和草图 | 已安装并启用 |

## 知识库治理增强

| 插件 | 用途 | 当前状态 |
| --- | --- | --- |
| Linter | 统一 Markdown、YAML、空行和格式规则 | 已安装并启用 |
| Metadata Menu | 管理属性字段、元数据菜单和结构化字段 | 已安装并启用 |
| Waypoint | 自动生成文件夹 MOC / 动态目录 | 已安装并启用 |
| Folder notes | 将文件夹和同名笔记绑定，支撑文件夹主页 | 已安装并启用 |
| Auto Note Mover | 按规则自动移动笔记到目标目录 | 已安装并启用，未配置自动移动规则 |
| Breadcrumbs | 构建父子、上下级、相关关系导航 | 已安装并启用 |

## 视觉与体验

| 插件 | 用途 | 当前状态 |
| --- | --- | --- |
| Minimal Theme Settings | Minimal 主题细节配置 | 已安装并启用 |
| Style Settings | CSS 变量和主题设置 | 已安装并启用 |
| Hider | 隐藏界面噪音 | 已安装并启用 |
| Iconic | 文件和界面图标增强 | 已安装并启用 |
| Recent Files | 最近文件侧栏/命令 | 已安装并启用 |

## 当前结论

- 插件目录数量：55
- 启用插件数量：55
- 当前没有发现“已启用但缺目录”的插件。
- 当前没有发现“已安装但未启用”的插件。
- 首页可用性主要依赖 `Dataview`、`Meta Bind`、`Tabs`、`Omnisearch`、`Recent Files`、`Hotkeys for specific files`。

## 排查顺序

1. 首页按钮不显示：检查 `Meta Bind` 是否启用。
2. 统计卡不显示：检查 `Dataview` 是否启用。
3. 底部 tabs 不显示：检查 `Tabs` 是否启用。
4. 搜索按钮无效：检查 `Omnisearch` 是否启用。
5. 最近文件按钮无效：检查 `Recent Files` 是否启用。
6. 固定页面按钮无效：检查 `Hotkeys for specific files` 的路径配置。
7. 属性字段不好管理：检查 `Metadata Menu` 是否启用。
8. 文件夹索引没有自动更新：检查 `Waypoint` 和 `Folder notes` 是否启用。
9. 自动移动文件前，必须先确认 `Auto Note Mover` 规则，避免误移动。
