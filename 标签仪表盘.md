---
type: 仪表盘
domain: 知识管理
status: 进行中
tags:
  - 领域/知识
  - 类型/汇总
---

# 📊 标签仪表盘

> 后台质量检查页，用来查看标签分布、待整理内容和 Dataview 筛选结果。日常入口仍以 [[HUB/Home]] 为准。

> 说明：本页依赖 Dataview 查询。未启用 Dataview 时，只把它作为标签体系和质量检查目录使用，不作为日常工作台。

---

## 🎯 快速统计

```dataview
TABLE WITHOUT ID
    "📈" AS "图标",
    length(rows) AS "数量",
    rows.file.link AS "最新笔记"
FROM #领域/技术
GROUP BY file.folder
LIMIT 5
```

---

## 📁 按领域分类

### 🔧 技术领域

```dataview
TABLE WITHOUT ID
    file.link AS "笔记",
    file.mtime AS "更新时间"
FROM #领域/技术
WHERE !contains(file.folder, "归档")
SORT file.mtime DESC
LIMIT 10
```

**统计**：
- 总笔记数：`$= dv.pages("#领域/技术").length`
- 重要笔记：`$= dv.pages("#领域/技术 AND #重要").length`
- 待复习：`$= dv.pages("#领域/技术 AND #待复习").length`

---

### 🤖 AI 领域

```dataview
TABLE WITHOUT ID
    file.link AS "笔记",
    file.mtime AS "更新时间"
FROM #领域/AI
WHERE !contains(file.folder, "归档")
SORT file.mtime DESC
LIMIT 10
```

**统计**：
- 总笔记数：`$= dv.pages("#领域/AI").length`
- 文章数量：`$= dv.pages("#领域/AI AND #类型/文章").length`
- 待复习：`$= dv.pages("#领域/AI AND #待复习").length`

---

### 💼 工作领域

```dataview
TABLE WITHOUT ID
    file.link AS "笔记",
    file.mtime AS "更新时间"
FROM #领域/工作
WHERE !contains(file.folder, "归档")
SORT file.mtime DESC
LIMIT 10
```

**统计**：
- 项目数量：`$= dv.pages("#领域/工作 AND #类型/项目").length`
- 进行中：`$= dv.pages("#领域/工作 AND #状态/进行中").length`
- 已完成：`$= dv.pages("#领域/工作 AND #状态/已完成").length`

---

### 🧠 知识领域

```dataview
TABLE WITHOUT ID
    file.link AS "笔记",
    file.mtime AS "更新时间"
FROM #领域/知识
WHERE !contains(file.folder, "归档")
SORT file.mtime DESC
LIMIT 10
```

**统计**：
- 知识库笔记：`$= dv.pages("#领域/知识").length`
- 重要内容：`$= dv.pages("#领域/知识 AND #重要").length`

---

### 💭 思维领域

```dataview
TABLE WITHOUT ID
    file.link AS "笔记",
    file.mtime AS "更新时间"
FROM #领域/思维
WHERE !contains(file.folder, "归档")
SORT file.mtime DESC
LIMIT 10
```

---

### 🏠 生活领域

```dataview
TABLE WITHOUT ID
    file.link AS "笔记",
    file.mtime AS "更新时间"
FROM #领域/生活
WHERE !contains(file.folder, "归档")
SORT file.mtime DESC
LIMIT 10
```

---

## 📋 按类型分类

### 📝 所有笔记

```dataview
TABLE WITHOUT ID
    file.link AS "笔记",
    file.tags AS "标签"
FROM #类型/笔记
WHERE !contains(file.folder, "归档")
SORT file.mtime DESC
LIMIT 15
```

---

### 📚 文章阅读

```dataview
TABLE WITHOUT ID
    file.link AS "文章",
    file.mtime AS "阅读时间"
FROM #类型/文章
WHERE !contains(file.folder, "归档")
SORT file.mtime DESC
LIMIT 10
```

---

### 📦 项目文档

```dataview
TABLE WITHOUT ID
    file.link AS "项目",
    file.mtime AS "更新时间"
FROM #类型/项目
WHERE !contains(file.folder, "归档")
SORT file.mtime DESC
```

---

### 🔧 资源参考

```dataview
TABLE WITHOUT ID
    file.link AS "资源",
    file.mtime AS "更新时间"
FROM #类型/资源
WHERE !contains(file.folder, "归档")
SORT file.mtime DESC
```

---

### 📊 总结汇总

```dataview
TABLE WITHOUT ID
    file.link AS "汇总",
    file.day AS "日期"
FROM #类型/汇总
WHERE !contains(file.folder, "归档")
SORT file.day DESC
LIMIT 10
```

---

## ⚠️ 状态管理

### 🔄 进行中

```dataview
TASK
FROM #状态/进行中
WHERE !completed
SORT priority DESC
```

---

### ⏳ 待处理

```dataview
TASK
FROM #状态/待处理
WHERE !completed
```

---

### ✅ 已完成（最近）

```dataview
TASK
FROM #状态/已完成
WHERE completed
SORT completedTime DESC
LIMIT 10
```

---

## 🌟 重要内容

### 高优先级笔记

```dataview
TABLE WITHOUT ID
    file.link AS "笔记",
    file.mtime AS "更新时间"
FROM #重要
WHERE !contains(file.folder, "归档")
SORT file.mtime DESC
LIMIT 15
```

---

## 📅 待复习内容

```dataview
TABLE WITHOUT ID
    file.link AS "需要复习",
    file.mtime AS "最后查看"
FROM #待复习
WHERE !contains(file.folder, "归档")
SORT file.mtime ASC
LIMIT 15
```

---

## 🎲 灵感卡片

```dataview
TABLE WITHOUT ID
    file.link AS "灵感",
    file.ctime AS "创建时间"
FROM #灵感
WHERE !contains(file.folder, "归档")
SORT file.ctime DESC
LIMIT 10
```

---

## 🔍 质量检查

### 缺少领域标签的笔记

```dataview
TABLE WITHOUT ID
    file.link AS "需要分类",
    file.folder AS "所在文件夹"
FROM -#领域/*
WHERE !contains(file.name, "00-") 
  AND !contains(file.folder, "Templates")
  AND !contains(file.folder, "Excalidraw")
SORT file.mtime DESC
LIMIT 20
```

---

### 标签过多的笔记（>8 个）

```dataview
TABLE WITHOUT ID
    file.link AS "笔记",
    length(file.tags) AS "标签数"
FROM 
WHERE length(file.tags) > 8
SORT length(file.tags) DESC
```

---

### 待整理的临时笔记

```dataview
TABLE WITHOUT ID
    file.link AS "待整理",
    file.mtime AS "修改时间"
FROM #待整理
SORT file.mtime ASC
```

---

## 📈 统计汇总

```dataview
TABLE WITHOUT ID
    "领域分布" AS "分类",
    length(rows) AS "数量"
FROM #领域/*
GROUP BY file.tags[0]
SORT length(rows) DESC
```

```dataview
TABLE WITHOUT ID
    "类型分布" AS "分类",
    length(rows) AS "数量"
FROM #类型/*
GROUP BY file.tags[0]
SORT length(rows) DESC
```

---

## 🎯 今日聚焦

### 今天需要完成的任务

```dataview
TASK
FROM #状态/进行中
WHERE !completed AND due = date("today")
```

### 今天创建的笔记

```dataview
TABLE WITHOUT ID
    file.link AS "笔记",
    file.tags AS "标签"
WHERE file.day = date("today")
```

---

## 💡 使用提示

1. **按需检查**：从 [[00-管理/00-管理索引]] 或 [[00-管理/知识库工作台]] 进入本页。
2. **定期清理**：优先查看"质量检查"部分，整理缺标签、标签过多和待整理笔记。
3. **任务筛选**：只把"状态管理"作为 Dataview 辅助视图，页面级任务仍看 [[HUB/Tasks]]。
4. **知识复习**：查看"待复习内容"，把需要行动的条目回链到项目、主题或收件箱。

---

## 🔗 相关文档

- [[00-管理/标签与属性速查]] - 当前最小标签和属性规则
- [[00-管理/知识库治理规范]] - 标签、链接、MOC 和归档规则
- [[00-管理/定期维护清单]] - 每日、每周、每月维护节奏
- [[00-管理/历史整理文档/00-标签体系使用指南]] - 历史完整说明，仅作参考
- [[00-管理/历史整理文档/00-标签速查卡片]] - 历史标签组合，仅作参考

---

*启用 Dataview 后，本仪表盘会自动更新；未启用插件时，请以当前治理规范和维护清单为准。*
